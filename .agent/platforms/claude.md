# Claude Code

- Root `CLAUDE.md` points at `.agent/CONTEXT.md` and `.agent/RULES.md`.
- Optional: `.claude/rules/` can mirror `.agent/rules/` if you want Claude-native rules trees.
- Graphify: `graphify install` for Claude / agents platforms per upstream docs.

Prefer editing `.agent/` over growing `CLAUDE.md`.
