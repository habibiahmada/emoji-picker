# Kiro

- Steering: `.kiro/steering/emoji-picker.md` (`inclusion: always`) points at `.agent/`.
- Root `AGENTS.md` is also picked up automatically by Kiro.
- Graphify: `graphify install --platform kiro`.

Custom agents must list resources explicitly, e.g. `file://.agent/**/*.md`.
